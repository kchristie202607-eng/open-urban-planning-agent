line-length = 100

[lint]
# Keep ruff focused on real bugs (F) and import hygiene (I).
# Broad style rules (E/BLE/S) are intentionally not selected; the CI step
# treats the selected correctness and import checks as blocking.
select = ["F", "I"]

[lint.per-file-ignores]
# Dev/utility script with intentional broad exception handling.
"scripts/inspect_git_objects.py" = ["BLE001"]
